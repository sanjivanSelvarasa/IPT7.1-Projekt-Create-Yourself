export type PortfolioType = {
  id: number,
  currentThemeId: number,
  currentVersionId: number,
  languageCode: string,
  userId: number,
  templateId: number,
  title: string,
  description: string,
  slug: string,
  visibility: string,
  createdAt: Date,
  updatedAt: Date,
}
