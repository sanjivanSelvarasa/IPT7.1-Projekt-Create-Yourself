export type ProjectType = {
  title: string,
  description: string,
  imageUrl?: string,
  projectUrl: string,
  githubUrl: string,
  sortOrder: number,
  startDate?: Date,
  endDate?: Date,
  createdAt: Date,
  updatedAt: Date,
  id: number,
}
